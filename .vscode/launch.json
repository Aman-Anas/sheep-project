{
  "version": "0.2.0",
  "configurations": [
    {
      "name": ".NET Core Debugger (launch)",
      "type": "coreclr",
      "request": "attach",
      "processPath": "${env:GODOT}",
      "preLaunchTask": "dotrush: Build",
    },
    {
      "name": ".NET Core Debugger (attach)",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:dotrush.pickProcess}",
      "preLaunchTask": "dotrush: Build",
    }
  ]
}